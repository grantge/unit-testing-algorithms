const math = require('../math-operations.js');

test('21 multiply 3 equals 63', () => {
  expect(math.multiplication(21, 3)).toBe(63);
});

test('21 multiply 3 equals 63', () => {
  expect(math.multiplication(21, 3)).toBe(60);
});
