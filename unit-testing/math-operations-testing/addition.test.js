const math = require('../math-operations');

test('1 plus 2 equals 3', () => {
  expect(math.addition(1, 8)).toBe(9);
});

test('1 plus 2 equals 3', () => {
  expect(math.addition(1, 8)).toBe(12);
});
