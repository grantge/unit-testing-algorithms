const math = require('../math-operations.js');

test('42 divide 2 equals 21', () => {
  expect(math.division(42, 2)).toBe(21);
});

test('42 divide 2 equals 21', () => {
  expect(math.division(42, 2)).toBe(45);
});
